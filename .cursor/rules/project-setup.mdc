---
description: Project setup and development environment configuration for the OC-Pipeline construction management system
alwaysApply: true
---

# Project Setup & Environment

## Quick Start

```bash
# Install dependencies
cd frontend && npm install
cd ../backend && npm install

# Start development servers
# Terminal 1 - Frontend
cd frontend && npm run dev

# Terminal 2 - Backend
cd backend && npm run dev
```

## Tech Stack

| Layer | Technology | Version |
|-------|------------|---------|
| Frontend | React + TypeScript | 18.3.x |
| Bundler | Vite | 5.4.x |
| Styling | Tailwind CSS | 3.4.x |
| UI Components | shadcn/ui | latest |
| Backend | Express + TypeScript | 4.x |
| Database | PostgreSQL (Supabase) | 15.x |
| ORM | Prisma | latest |
| Auth | Supabase Auth | 2.x |

## Project Structure

```
oc-pipeline/
├── frontend/           # React SPA
│   ├── src/
│   │   ├── components/ # UI components
│   │   │   ├── ui/     # shadcn/ui primitives
│   │   │   ├── layout/ # App shell components
│   │   │   └── dashboard/ # Dashboard widgets
│   │   ├── pages/      # Route components
│   │   ├── hooks/      # Custom hooks + contexts
│   │   ├── services/   # API layer
│   │   ├── utils/      # Helpers
│   │   └── types/      # TypeScript definitions
│   └── public/         # Static assets
├── backend/            # Express API
│   ├── src/
│   │   ├── controllers/
│   │   ├── routes/
│   │   ├── middleware/
│   │   └── config/
│   └── prisma/         # Database schema
├── database/           # SQL migrations
└── docs/               # Documentation
```

## Path Aliases

Frontend uses `@/` alias pointing to `src/`:
```typescript
import { Button } from '@/components/ui/button'
import { useAuth } from '@/hooks/AuthContext'
```

## Environment Setup

Create `.env` files:

```bash
# frontend/.env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# backend/.env
DATABASE_URL=postgresql://user:pass@host:5432/db
SUPABASE_SERVICE_ROLE_KEY=your-service-key
PORT=3001
```

## Common Commands

```bash
# Development
npm run dev          # Start dev server
npm run build        # Production build
npm run preview      # Preview production build
npm run lint         # Run ESLint

# Database (backend)
npx prisma generate  # Generate Prisma client
npx prisma migrate   # Run migrations
npx prisma studio    # Open Prisma Studio
```

$END$

*Context added by Giga project-setup - Development environment configuration and project structure overview.*
