# MGX Configuration File
project:
  name: oc-pipeline-frontend
  type: frontend
  framework: react-vite

scripts:
  dev:
    command: vite
    port: 5173
  build:
    command: tsc && vite build
  preview:
    command: vite preview